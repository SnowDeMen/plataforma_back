# ===========================================
# CONFIGURACION DEL BACKEND - PLATAFORMA ENTRENAMIENTOS
# ===========================================
# Copia este archivo a .env y configura los valores
# cp .env.example .env

# ===========================================
# ENTORNO: development | production
# ===========================================
# development: Debug activo, logs detallados, Selenium con GUI
# production: Optimizado, headless, logs reducidos
ENVIRONMENT=development

# ===========================================
# SERVIDOR
# ===========================================
HOST=0.0.0.0
PORT=8000
DEBUG=true
LOG_LEVEL=DEBUG

# ===========================================
# BASE DE DATOS POSTGRESQL
# ===========================================
# Opcion 1: Componentes separados (recomendado para flexibilidad)
# El sistema construye la URL automaticamente
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=training_user
DATABASE_PASSWORD=tu_password_seguro
DATABASE_NAME=training_db

# Opcion 2: URL completa (tiene prioridad sobre los componentes)
# Descomenta y usa esta opcion si prefieres especificar la URL completa
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database

# Configuracion del pool de conexiones
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# ===========================================
# TRAININGPEAKS (credenciales de la cuenta)
# ===========================================
TP_EMAIL=tu_email@ejemplo.com
TP_PASSWORD=tu_password_trainingpeaks

# ===========================================
# OPENAI / LLM
# ===========================================
OPENAI_API_KEY=sk-...
AUTOGEN_MODEL=gpt-4o-mini
AUTOGEN_TEMPERATURE=0.7
AUTOGEN_MAX_TOKENS=128000

# ===========================================
# SELENIUM (Navegador Chrome)
# ===========================================
# En desarrollo: false para ver el navegador y debuggear
# En produccion: true (headless, sin GUI)
SELENIUM_HEADLESS=false

# ===========================================
# CORS (Cross-Origin Resource Sharing)
# ===========================================
# Desarrollo: permitir localhost del frontend
# Produccion: URL especifica de tu dominio o * para todos
# Acepta formato JSON array o valores separados por coma
CORS_ORIGINS=["http://localhost:3001", "http://127.0.0.1:3001"]

# ===========================================
# SEGURIDAD
# ===========================================
# IMPORTANTE: Cambiar estos valores en produccion
SECRET_KEY=dev-secret-key-cambiar-en-produccion
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# WORKOUTS
# ===========================================
WORKOUTS_DIR=workouts
DEFAULT_WORKOUT_LIBRARY=Neuronomy

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_PER_MINUTE=60

# ===========================================
# NOTAS DE CONFIGURACION
# ===========================================
#
# DESARROLLO LOCAL:
# 1. cp .env.example .env
# 2. Editar .env con tus credenciales reales
# 3. Levantar Postgres: docker-compose -f docker-compose.dev.yml up -d
# 4. Ejecutar backend: cd api && python main.py
# 5. Ejecutar frontend: cd plataforma_front && npm run dev
#
# PRODUCCION (Easypanel):
# - ENVIRONMENT=production
# - SELENIUM_HEADLESS=true
# - DATABASE_HOST=nombre-servicio-postgres
# - SECRET_KEY=clave-segura-generada
# # ===========================================
# AIRTABLE SYNC (Opcional, activa el job programado)
# ===========================================
# AIRTABLE_TOKEN=tu_token_personal_de_airtable
# AIRTABLE_BASE_ID=tu_base_id
# AIRTABLE_TABLE_NAME=NombreDeLaTabla
# AIRTABLE_LAST_MOD_FIELD=NombreDelCampoModifiedTime
# AIRTABLE_SYNC_INTERVAL_HOURS=12
# AIRTABLE_PG_SCHEMA=airtable
# AIRTABLE_PG_TABLE=
#
# ===========================================
# GENERACION AUTOMATICA DE ENTRENAMIENTOS
# ===========================================
# ATHLETE_TRAINING_GEN_INTERVAL_HOURS: Frecuencia de revisión (defecto 12h)
ATHLETE_TRAINING_GEN_INTERVAL_HOURS=12.0
# ATHLETE_TRAINING_GEN_THRESHOLD_DAYS: Revisar atletas que no han tenido generación en X días
ATHLETE_TRAINING_GEN_THRESHOLD_DAYS=28
# ATHLETE_TRAINING_GEN_WEEKS: Semanas a generar en el plan
ATHLETE_TRAINING_GEN_WEEKS=4

# ===========================================
# AUTHENTICATION
# ===========================================
AUTH_USERNAME=""
AUTH_PASSWORD=""
